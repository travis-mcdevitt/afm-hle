[project]
name = "afm-hle"
version = "0.1.0"
description = "Resumable HLE evaluation through the AFM monitoring gateway"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
data = ["huggingface-hub>=0.34,<2", "pyarrow>=18,<24"]

[project.scripts]
afm-hle = "afm_hle.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["afm_hle*"]
